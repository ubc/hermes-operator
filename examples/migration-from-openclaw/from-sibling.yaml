apiVersion: hermes.agent/v1
kind: HermesInstance
metadata:
  name: migrated-from-sibling
spec:
  image:
    repository: ghcr.io/paperclipinc/hermes-agent
    tag: "v2026.5.29.2"
  storage:
    persistence:
      enabled: true
      size: 20Gi
  migration:
    fromOpenClaw:
      source:
        openclawInstanceRef:
          name: my-openclaw
          namespace: agents
      mode: copy
