apiVersion: hermes.agent/v1
kind: HermesInstance
metadata:
  name: minimal
spec:
  image:
    repository: ghcr.io/ubc/hermes-agent
    tag: "v2026.5.29.2"
  storage:
    persistence:
      enabled: true
      size: 10Gi
