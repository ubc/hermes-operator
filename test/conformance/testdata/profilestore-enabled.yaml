apiVersion: hermes.agent/v1
kind: HermesInstance
metadata:
  name: conformance-profilestore
spec:
  image:
    repository: ghcr.io/ubc/hermes-agent
    tag: "v0.16.0"
  storage:
    persistence:
      enabled: true
      size: 1Gi
  profileStore:
    honcho:
      enabled: true
      apiKeySecretRef:
        name: api-keys
        key: honcho-api-key
      persistence:
        enabled: true
        size: 1Gi
